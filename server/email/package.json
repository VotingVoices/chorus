{
  "name": "email",
  "version": "1.0.0",
  "description": "Lambda that is invoked by the client to email a vote plan to the user",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.13",
    "serverless-offline": "^3.28.0",
    "serverless-plugin-typescript": "^1.1.5"
  }
}
